name: viggo-frontend

services:
  frontend:
    container_name: viggo-frontend
    image: ${FRONTEND_IMAGE:-viggo-web:local}
    build:
      context: .
      dockerfile: Dockerfile
      args:
        APP_ENV: ${APP_ENV:-prod}
      secrets:
        - frontend_env
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-5173}:80"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "wget -qO- http://127.0.0.1/ >/dev/null 2>&1 || exit 1",
        ]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s

secrets:
  frontend_env:
    file: ${FRONTEND_ENV_FILE:-.env.prod}
